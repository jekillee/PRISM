# PRISM Dependencies — Plasma Research Integrated System for Multi-diagnostics
#
# Production install is via setup_vendor.sh (pinned to Linux x86_64 + Py 3.8,
# bundled into vendor/). This file documents the same package set for clarity
# and as a fallback for ad-hoc environments:
#     pip install -r requirements.txt

# GUI framework (PySide6 / Qt 6)
PySide6>=6.2.4

# Core scientific stack
numpy>=1.20.0
scipy>=1.7.0
matplotlib>=3.5.0

# Image processing (TV / IRVB tabs)
Pillow>=8.0.0

# TRANSP CDF (NetCDF-3 + NetCDF-4) loading
netCDF4>=1.5.0

# Profile fitting — GPR backend (optional but recommended)
scikit-learn>=1.0.0

# KSTAR MDS+ connection (Python bindings; system libMdsShr.so still required)
MDSplus>=7.0.0

# Raw-Mirnov NAS archive (HDF5). h5py covers lzf/gzip; hdf5plugin adds the
# blosc-zstd filter used by default (fastest write + smallest files).
h5py>=3.1.0
hdf5plugin>=3.0.0
